{
  "name": "muse-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Muse — Subsonic-compatible music server, web UI",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^25.6.2",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
