# ---- Web framework ----------------------------------------------------------
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# ---- Settings ---------------------------------------------------------------
pydantic>=2.5.0
pydantic-settings>=2.1.0
PyYAML>=6.0

# ---- Auth -------------------------------------------------------------------
bcrypt==5.0.0
python-jose[cryptography]==3.5.0

# ---- Database ---------------------------------------------------------------
# sqlite3 ships with the Python stdlib — no entry here. psycopg3 is the
# Postgres driver and ConnectionPool, used when database_url is set to a
# postgresql:// URL. [binary] avoids needing libpq-dev at install time.
psycopg[binary,pool]>=3.1.0

# ---- Audio metadata ---------------------------------------------------------
mutagen==1.47.0
librosa==0.11.0
numpy==2.4.6

# ---- Image processing -------------------------------------------------------
# Used for on-the-fly cover-art resizing in getCoverArt. Pillow's bundled
# libjpeg/libpng/libwebp covers every format we sniff in scanner/artwork.py.
Pillow>=12.0.0

# ---- HTTP utilities ---------------------------------------------------------
python-multipart>=0.0.20  # required by FastAPI for form uploads (future use)
slowapi>=0.1.9

# ---- Dev / optional ---------------------------------------------------------
# pytest>=8.0.0           # uncomment for tests
# httpx>=0.26.0           # uncomment for test client
